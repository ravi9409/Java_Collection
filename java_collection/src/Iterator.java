import java.util.*;

public class Iterator {
    public static void main(String[] args) {
        /*// Create a list and add elements to it
        List<String> list = new ArrayList<>();
        list.add("Java");
        list.add("Python");
        list.add("C++");

// Get an iterator and skip the first element
        java.util.Iterator<String> iterator = list.iterator();
        iterator.next(); // Skip the first element

// Perform action for each remaining element
        iterator.forEachRemaining(System.out::println);*/


       /* List<String> list = new ArrayList<>();
        list.add("Java");
        list.add("Python");
        list.add("C++");

        Iterator<String> iterator = list.iterator();
        while (iterator.hasNext()) {
            String element = iterator.next();
            if ("Python".equals(element)) {
                iterator.remove(); // Remove the element "Python"
            }
        }

        // Print the list after removal
        list.forEach(System.out::println);*/

    }
}
