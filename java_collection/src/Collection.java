import java.util.*;

public class Collection {
    public static void main(String[] args) {
        /*// Create an ArrayList of Integer and add elements to it
        ArrayList<Integer> collection = new ArrayList<>();
        collection.add(1);
        collection.add(2);
        collection.add(3);
        collection.add(4);
        // Convert the ArrayList to an Integer array using a generator function
        Integer[] array = collection.toArray(Integer[]::new);
        // Convert the Integer array to an int array
        int[] intArray = new int[array.length];
        for (int i = 0; i < array.length; i++) {
            intArray[i] = array[i];
        }*/
    }

}